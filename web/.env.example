# Base URL of the Invidious instance whose JSON API this frontend consumes.
# Matches the host port exposed by the repo-root docker-compose.yml (127.0.0.1:3000:3000).
INVIDIOUS_API_BASE_URL=http://localhost:3000
